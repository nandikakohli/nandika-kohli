# Archived React/Vite App

This folder contains the older React/Vite version of the website for reference.

The active site is `../../svelte-website`. Use the root `package.json` scripts or run commands directly in `svelte-website` for current development.
